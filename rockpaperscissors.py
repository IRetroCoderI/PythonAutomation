import sys, random

active = True

options = ["rock", "paper", "scissors"]

robot = random.choice(options)

while active:
    print("ROCK PAPER SCISSORS BITCHES")
    user = input("(r)ock, (p)aper, (s)cissors, or (q)uit? ")
    if user == 'r':
        
    if user == 'p':

    if user == 's':

    if user == "q":
        break

print("goodbye!")